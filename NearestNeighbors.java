// Hausübung 1 Sina Moshtaghi 

package classifier;

import java.util.ArrayList;

public class NearestNeighbors {
	private final ArrayList<Object[]> date = new ArrayList<Object[]>();
	
	public void learn(int nameLength, int nameDigits, boolean label) {
		this.data.add(new Object[] {nameLength, nameDigits, label}); 
		
	}
	
	public boolean classify(String name)
	    return ((boolean) this.data.get((int) (double)Math.pow((int)n[0] - s.length(), 2) + Math.pow((int, arg1)this.data.get[1] - s.ReplaceAll()
	
	
	
	
	String[] names = {"kater", "xmb", "chazette", "12345IchKaufDeinAuto12345","Buy1234Buy1234Buy1234"};
	
	for (String string : names) {
		System.out.println( string + " ist " + (nn.classify(string) ? "ein" : "kein") + " Spammer.");
	    
		}
	}

}
